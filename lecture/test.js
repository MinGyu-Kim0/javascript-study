// Java
class Bar {
    // Constructor
    Bar() {}
    Bar(int x) {}
}
Bar obj = new Bar(2); // 실제론 2번 나눠서 실행됨.
// 만들어진 객체를 참조해서 멤버 변수를 읽거나
// new연산자로 객체를 만들면 현재 생성된 객체의 주소가 넘어와야함
// 그 주소를 참조변수가 받아야함.
// 이때 생성자가 값을 리턴하면 안됨 그래서 반환형이없다.

// Python
class Bar:
// Constructor    
    def __init__():
    ...
// JavaScript
class Bar { // ES6
    
    // 클래스 구성요소
    // Constructor
    constructor() {
        
    }
    // Member variable
    // Member method
    // Destructor
    
}

function Bar() { //constructor function, Legacy

}